local data = {
    userKey = nil
    key = nil
}

function data.checkKey()
    if data.userKey ~= data.key then
        game:GetService("Players").LocalPlayer:Kick("incorrect or missing key, join our discord for beta - auto.projectbot.top")
    end
end

return data
